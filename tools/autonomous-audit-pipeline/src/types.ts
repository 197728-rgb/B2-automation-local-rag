/**
 * @deprecated Import from ./schemas.js — kept for older imports.
 * Python production uses `text` / `automationStatus` on SynthesizedAnswer; TS reference uses rawAnswer/formattedAnswer.
 */
export * from "./schemas.js";
