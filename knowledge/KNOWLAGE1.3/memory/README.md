# Retained Memory

Snapshots of historical knowledge that exists outside the project repository. Frozen at
build time and last in precedence — see `../README.md`.

| Path | What it is | Precedence note |
|---|---|---|
| `b2s-platform-SKILL.md` | Operations guide for the B2S Enterprise Compliance Platform: file map, SOP 0–10 pipeline, activity codes, output layout, error catalogue, and the three-tool bundle | A **different product** from `B2-automation-local-rag`. Neither governs the other; see rule 2 in `../KNOWLEDGE_SOURCES_INDEX.md` |
| `drive/DRIVE_KNOWLEDGE_INDEX.md` | Index of the Google Drive knowledge store as observed on 2026-08-31, with file IDs | An index of what exists, not an endorsement of any copy as canonical. Several entries are known duplicates (C-6) |
| `drive/B-2_Master_Schema.json` | AAR M-1002 Exhibit B-2 master field schema: canonical field names and their label aliases across activity codes | A **discovery aid only**. It never overrides the current controlled form, and it holds no coordinates |

None of these files carries facility data.

## Redactions

`b2s-platform-SKILL.md` is reproduced verbatim except for one illustrative car mark in
its "add a new activity code" example, replaced with `<CAR MARK REDACTED>`. The example
teaches the shape of a field-set entry; the car number carried no method meaning and
would have tripped the builder's evidence gate. No other snapshot content was altered.
