"""Layer 1 - The Form Brain.

Reads the blank form like an auditor: builds the Form Obligation Graph,
classifies required/optional/never-write fields, and loads the exact
write-authority map. Refuses 'nearest', 'latest', 'similar'.
"""
